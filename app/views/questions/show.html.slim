.question
  p = @question.title
  p = @question.body
  p = link_to 'Edit', edit_question_path(@question) if @question.user == current_user
  p = link_to 'Delete', @question, method: :delete if @question.user == current_user
= render @question.answers
= render "answers/form", question: @question, answer: @answer if user_signed_in?
